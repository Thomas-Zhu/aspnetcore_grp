syntax = "proto3";

option csharp_namespace = "GrpcBook";

package race;

service Racer {
  rpc ReadySetGo (stream RaceMessage) returns (stream RaceMessage);
}

message RaceMessage {
  int32 count = 1;
}